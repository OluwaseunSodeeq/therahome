export type Step = {
  num: number;
  icon: string;
  title: string;
  desc: string;
};

export type BookingPrice = {
  label: string;
  price: string;
  description: string;
};

export type TransportZone = {
  zone: string;
  area: string;
  fee: string;
};
